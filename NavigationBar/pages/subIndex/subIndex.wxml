<!--pages/subIndex.wxml-->
<navigationBar 
id="gainPhone" 
text="{{nav.title}}" 
back="{{nav.back}}"
home="{{nav.home}}"
>
</navigationBar>


<button bindtap='clickThirdIndex'>点击跳转到3级页面</button>

<text>我是"{{nav.title}}"\n是否有返回按钮"{{nav.back}}"\n是否有home按钮"{{nav.home}}"</text>