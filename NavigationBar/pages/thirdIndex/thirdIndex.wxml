<!--pages/thirdIndex/thirdIndex.wxml-->
<navigationBar 
id="gainPhone" 
text="{{nav.title}}" 
back="{{nav.back}}"
home="{{nav.home}}"
>
</navigationBar>

<text>我是"{{nav.title}}"\n是否有返回按钮"{{nav.back}}"\n是否有home按钮"{{nav.home}}"</text>